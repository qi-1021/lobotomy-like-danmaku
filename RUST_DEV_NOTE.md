# Rust 版本开发提醒

## 同步规则

**Python 版本是主版本，Rust 版本是衍生版本。**

- 所有新功能、修复、改进**优先在 Python 版本中实施**
- Rust 版本通过**每版本一同步**或**几版本一同步**的方式，从 Python 版本移植相同功能
- 不要在 Rust 版本中独立添加 Python 版本没有的功能（除非是 Rust 特有的性能优化）

## 同步流程

1. 在 Python 版本完成功能开发并验证
2. 记录变更内容（新增参数、UI 变化、行为修改）
3. 在 Rust 版本中对应修改 core / cli / gui
4. 确保 Rust 版本行为与 Python 版本一致

## 分支结构

- `main` — Python 版本（主）
- `rust-version` — Rust 版本（衍生）

## 当前状态

Rust 版本已实现：
- [x] 核心引擎（渲染、叠加生成、视频处理）
- [x] CLI（功能完整）
- [x] GUI（基础框架，功能待完善）

待从 Python 同步：
- [ ] 预设文字列表功能（Python 已删除，Rust 也应删除）
- [ ] 项目保存/加载的完整兼容
- [ ] GUI 细节打磨（颜色选择器、布局微调等）
